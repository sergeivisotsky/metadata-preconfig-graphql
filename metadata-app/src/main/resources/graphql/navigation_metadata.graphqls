type Navigation {
    type: NavigationType
    numberOfElements: Int
    fixed: Boolean
    resizable: Boolean
    elements: [NavigationElement]
}

type NavigationElement {
    code: String
    value: String
    isActive: Boolean
}